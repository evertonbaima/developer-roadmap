class Car {
    constructor(make, model, numDoors, colour) {
        this.make = make;
        this.model = model;
        this.numDoors = numDoors;
        this.colour = colour;
    }
}

module.exports = Car;